## 如何提交修改

为了保证仓库内容不会被误改，本项目要求所有对规则、说明等内容的修改都通过 **Pull Request（拉取请求）** 提交，而不是直接修改主分支。

简单来说：

1. 如果你想提出修改，先 **Fork（复制）** 本仓库到你的 GitHub 账户。
2. 在你的仓库里创建一个新的分支，在该分支上进行修改。
3. 修改完成后，提交一个 **Pull Request** 给本仓库。
4. 创作者会审核你的改动内容，并决定是否合并。

通过 Pull Request 提交修改可以让作者先查看、确认更改内容，然后再把它合并进主仓库，这样可以避免无意的错误或冲突。